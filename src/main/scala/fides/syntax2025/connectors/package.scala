package fides.syntax2025

/**
  * Connector primitives provide ways to control the flow of data. They do not control execution.
  * So a typical if-then-else statement, for example would not be a pure connector,
  * because it controls which branch gets executed.
  */
package object connectors
